export interface CarouselItem {
    image: string
    name: string
    floor: string
  }
  
  export interface NFTMetadata {
    name: string
    symbol: string
    description: string
    image: string
  }